# AGENTS

## 项目协作约定

- 本仓由一名维护者与 AI 长期维护；使用中文回复、撰写文档和提交摘要。
- 开始修改前阅读根 `README.md` 和目标子工程 `README.md`，核对工作树与 `commit-message.txt`。保留已有改动，修改后在该文件追加不重复的中文变更摘要。
- 项目边界规则见 [Container 规则](.agents/esp-container.md)。用户当前要求与真实源码、配置优先于旧说明；未验证的行为只记为进行中。
- 独立构建只依赖本仓和明确锁定的公开 SDK、组件，不从相邻工作区或私有 Tool 读取运行源码。默认分支为 `master`。
- 节点职责、入口或依赖改变时，同步对应 `README.md` 的 Mermaid 架构拓扑。普通文档 basename 用 kebab-case，C/ESP-IDF 源文件与标识符用 snake_case。
- 真实设备写入前必须核对当次设备身份、分区和两份完整恢复基线；不得擅自擦除身份配置、写 eFuse 或驱动真实 GPIO。主机构建和模拟测试不能替代 C3 实板验收。
- 公开仓不保存签名私钥、设备身份、网络凭据或 Flash 恢复字节；已有有效凭据不因时间或私有会话读取而更换。
