- 将锁定 ESP-IDF 切至公开 OTA 句柄修复 fork，并更新来源说明
- 记录五组件 QEMU 128 KiB 失败与 64 KiB 容量切片
- 新增 product.pkg 只读流式验包切片与主机故障测试
- 新增 product.pkg 设备端只读 Wasm ABI 与独立授权静态检查
- 新增按精确专用分区与 NVS 合同绑定的三槽 ESP-IDF provider，并补齐宿主及 C3 构建验证
- 修正超长 Flash 读写长度的整数回绕边界并增加对应负例
- 增加 WAMR 生命周期百次回归与经校准的宿主资源用量检查，明确 P6-09 未验收边界
- 增加实际双固件集合的三槽对账和容量保护主机测试，明确联合 OTA 仍需实测状态输入
- 记录当前 Base v3 签名五组件 QEMU 的 64 KiB guest 堆余量与容量风险
- 记录当前 FRP 会话单次内存需求与 guest 同时运行的 QEMU 容量冲突
- 记录五组件 S3 仓外静态构建、分区与签名证据及未验收边界
- 将候选包槽 Flash 回读接入签名、Wasm 与独立产品和资源授权，绑定持久操作并增加主机和固定 SDK 验证
- 区分包槽二次 Flash 读取失败与完整内容信任拒绝，保留原始 I/O 故障语义
- 增加受配额定时器、实例代次句柄和显式到期投递，并补齐签名能力检查与 WAMR/C3 验证
- 修复定时器句柄 24 位序号耗尽，使用跨实例单调 64 位分配并验证边界与取消后不复用
- 收敛主机打包初筛与设备 Wasm 流式准入的确定性 Classic/ABI 规则，并用同一批签名负例交叉验证
- 将 Container 固定 SDK 更新到公开 HTTP 初始化失败句柄修复提交，保留历史验证记录
- 给组件私有 guest 入口增加单调期限裁决，拒绝超期结果与后续原生副作用并补齐真 Wasm 回归
